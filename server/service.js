'user stric';

const express = require('express'); // Import 'express' package into a variable called express
const service = express(); // Top-level function (fort of like an init or constructor). Creates an express application

module.exports = service;